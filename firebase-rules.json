{
  "rules": {
    "u": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "config": {
      "iaKey": {
        ".read": "auth != null",
        ".write": "auth != null && auth.token.email === 'lokoporgame194@gmail.com'"
      }
    }
  }
}
